const mdLinks = require('./mock_mdlinks');

// eslint-disable-next-line no-undef
describe('mdLinks', () => {
  // eslint-disable-next-line no-undef
  it('should return a table', () => {
    
  });
});
